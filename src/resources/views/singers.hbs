<h3 style="text-align: center;">CA SĨ</h3>

    <div class="singerhbs">
        {{#each singers}}
            <a href="/singers/{{this.singer_id}}" class="singerInfor">
                <div class="singer-bg" style="background-image: url({{this.singer_image}})"></div>
                <p>{{this.singer_name}}</p>
            </a>
        {{/each}}
    </div>
